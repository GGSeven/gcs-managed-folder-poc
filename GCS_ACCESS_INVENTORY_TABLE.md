# GCS 访问权限盘点与管控需求确认表（客户填写）

> **目标存储桶**：`gs://fr-xjsy-bigdata-gcs-dev`  
> **填写目的**：精确划定计算组件对 GCS 目录的权限边界，区分**公共依赖**与**业务冷热分区**，避免权限调整导致 Spark/Trino/ETL 运行中断。

---

## 一、管控范围前置确认

| # | 确认项 | 当前掌握情况 | 客户核对确认（请选填） |
|---|---|---|---|
| **0** | **【核心管控范围确认】** | 经讨论，当前策略**仅针对核心大表** `datasets/ods.db/ods_can_origin_data_1s_p_t_i/` 进行 60 天冷热访问隔离；其他所有表保持原样，不设拦截。 | [ ] 确认仅管控此单表<br>[ ] 需同时管控其他表（请在下表补充） |

---

## 二、组件访问目录清单（权限盘点主表）

### 填写规则说明：
1. **组件**：执行任务的引擎或平台（如 Kyuubi、Spark、Trino、DolphinScheduler、Flink、Hive Metastore）。
2. **访问目录（桶内前缀）**：组件需要读取或写入的 GCS 具体路径。一个组件访问多个目录请分行填写。
3. **读/写**：明确该目录是 `读`、`写` 还是 `读写`。
4. **冷/热范围**：
   - **非管控目录（公共依赖/其他表）**：直接填 `—`；
   - **管控表（`ods_can_origin_data_1s_p_t_i`）**：
     - `热`：日常分析查询，只需访问近 60 天内数据；
     - `冷`：历史归档查询，只需访问 60 天以前数据；
     - `冷+热`：写入、补数或运维维护作业，需要跨全生命周期访问。

---

### 盘点明细表

| 序号 | 所属分类 | 计算组件 | 访问的 GCS 目录（桶内前缀） | 读/写 | 访问冷/热数据 | 状态与核对 |
|:---:|---|---|---|:---:|:---:|---|
| **1** | **公共系统依赖** | Kyuubi Server | `user/spark/kyuubi-spark-sql-engine_2.12-1.9.3.jar` | **读** | — | [x] 已预填 (请核对) |
| **2** | **公共系统依赖** | Spark 引擎 (Kyuubi 拉起) | `user/spark/`（含 pod 模板等 yaml） | **读** | — | [x] 已预填 (请核对) |
| **3** | **公共系统依赖** | Spark 引擎 | `flink_jar/iceberg-hive-runtime-1.4.2.jar` | **读** | — | [x] 已预填 (请核对) |
| **4** | **公共系统依赖** | Spark 引擎 | `spark-job-history/`（运行时写 EventLog） | **写** | — | [x] 已预填 (请核对) |
| **5** | **公共系统依赖** | Spark History Server | `spark-job-history/`（界面展示作业历史） | **读** | — | [x] 已预填 (请核对) |
| **6** | **公共系统依赖** | Spark 引擎 | `datasets/`（临时作业文件 `upload.path`，*建议迁出*） | **写** | — | [x] 已预填 (请核对) |
| **7** | **未管控业务数据** | Spark 引擎 (通用 ETL) | `datasets/`（除本次被管控表以外的常规数据目录） | **读写** | — | [x] 已预填 (请核对) |
| **8** | **管控大表-元数据** | Spark 查询 / Trino | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/metadata/` | **读** | **冷 + 热**<br>(元数据必须全程可读) | [x] 已预填 (请核对) |
| **9** | **管控大表-分析查询**| **Spark 引擎 (员工查询)** | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/data/` | **读** | **热**<br>(近 60 天数据) | [ ] 待客户确认 |
| **10**| **管控大表-分析查询**| **Trino (员工查询)** | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/data/` | **读** | **热**<br>(近 60 天数据) | [ ] 待客户确认 |
| **11**| **管控大表-归档查询**| **历史审计 / 归档查询** | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/data/` | **读** | **冷**<br>(60 天以前数据) | [ ] 待客户确认 |
| **12**| **管控大表-实时写入**| **ETL 同步 / 实时写入** | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/` | **读写** | **冷 + 热**<br>(全量写入/更新) | [ ] 待客户确认 |
| **13**| **管控大表-表维护** | **Compaction / 快照清理**| `datasets/ods.db/ods_can_origin_data_1s_p_t_i/` | **读写** | **冷 + 热**<br>(重写/合并历史分区) | [ ] 待客户确认 |
| **14**| **元数据服务** | Hive Metastore | *(请补充该服务访问 GCS 的具体路径)* | | | [ ] 请补充填写 |
| **15**| **流式写入** | Flink 作业（如存在） | *(请补充 Flink Checkpoint/数据写入路径)* | | | [ ] 请补充填写 |
| **16**| **其他** | BI 工具 / 运维人工操作 | *(请补充其他直接读写 GCS 的系统)* | | | [ ] 请补充填写 |

---

## 三、实施前必须明确的 4 个技术问题

| # | 问题项 | 为什么重要（影响分析） | 客户技术团队答复 |
|:---:|---|---|---|
| **1** | **Service Account (SA) 归属与解耦**<br>各组件目前运行使用的是哪个 GCP SA？哪些组件共用同一个？ | 员工日常查询（只读热数据）与 ETL 写入（全量读写）**必须使用不同 SA**。如果共用同一个 SA，一旦收紧权限会导致 ETL 写入中断。 | 答： |
| **2** | **分区日期判定字段**<br>针对被管控表，冷热按哪个字段划分 60 天：<br>A. `server_dt_utc`（服务端落盘 UTC 日期）<br>B. `car_dt_utc`（车机端实际上报 UTC 日期）？ | 自动化调和脚本需要依据该分区目录的日期与当前时间计算天数差，决定划入 Hot 还是 Cold。 | 答： |
| **3** | **是否有定期小文件合并与维护作业**<br>是否有 Spark/Flink 定期跑 `rewrite_data_files` (Compaction) 或 `expire_snapshots`？ | 维护作业需要重写甚至物理删除历史冷分区的小文件。**运行此作业的账号必须拥有全量（冷+热）读写权限**，严禁使用查询用的 hot-reader。 | 答： |
| **4** | **Spark 临时文件上传路径优化**<br>当前 `spark.kubernetes.file.upload.path` 配置为 `datasets`。能否将该临时路径迁出至非业务目录（例如 `tmp/spark-uploads/`）？ | `datasets/` 根目录将被收紧为业务只读。若 Spark 任务提交时向其上传临时 jar/conf，会触发 403 导致任务直接提交失败。 | 答： |

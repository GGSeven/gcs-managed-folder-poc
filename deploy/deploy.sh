#!/usr/bin/env bash
# ==============================================================================
# 兼容引导脚本：自动调用项目根目录下的全自动化部署脚本 deploy.sh
# ==============================================================================
set -euo pipefail
cd "$(dirname "$0")/.."
exec ./deploy.sh "$@"

# GCS 访问权限与组件调用盘点表

> **目标存储桶**：`gs://fr-xjsy-bigdata-gcs-dev`  
> **填写目的**：全面梳理所有会访问 GCS 的组件、作业与工具，精确界定各服务的路径访问范围，确保 60 天冷热隔离生效的同时，不影响生产任务与引擎运行。

---

### 📋 填写说明与规则

1. **示例说明**：下表中前 4 行为**参考示例**（基于此前沟通提取），**并不代表当前生产环境的全部组件**。请结合实际运行环境，将所有会读写该 GCS 桶的组件、调度作业、同步工具等逐一补充完整，避免遗漏导致权限收紧后任务异常。
2. **读/写**：填写 `读`、`写` 或 `读写`；
3. **冷/热数据区分**：
   - 仅当访问涉及本次管控的大表（`ods_can_origin_data_1s_p_t_i`）时填写：
     - `热`：仅需访问近 60 天内数据；
     - `冷`：仅需访问 60 天前数据；
     - `冷+热`：历史全生命周期数据均需访问；
   - 访问公共目录、依赖包、系统日志或其他业务表时，统一填 `—`；
4. **服务账号 (SA)**：请务必明确写出该组件当前实际使用的 GCP Service Account 邮箱。

---

## 一、GCS 访问路径盘点表（前 4 行为示例，其余请根据实际情况补充）

| 序号 | 业务场景 / 组件名称 | 访问的目标目录（桶内前缀路径） | 操作类型 | 数据范围 | 当前实际运行的 SA 账号 | 备注说明 |
|:---:|---|---|:---:|:---:|---|---|
| *示例* | Spark / Kyuubi 运行依赖 | `user/spark/`、`flink_jar/` | 读 | — | *(示例: xxx-dev@...)* | 引擎启动所需的公共 JAR 包与模板 |
| *示例* | Spark 日志与上传临时区 | `spark-job-history/` | 读写 | — | *(示例: xxx-dev@...)* | 运行日志与作业临时上传区 |
| *示例* | 交互式 SQL 查询 (如 Hue/Trino) | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/data/` | 读 | **热** | *(示例: iceberg-hot-reader@...)* | 分析师只查近 60 天，严禁查冷数据 |
| *示例* | 定时离线 ETL (如调度同步/清洗) | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/` | 读写 | **冷+热** | *(示例: iceberg-writer@...)* | 数据同步管道，需全生命周期读写 |
| **1** | *(请补充实际组件，如 Trino)* | | | | | |
| **2** | *(请补充实际组件，如 DolphinScheduler 作业)* | | | | | |
| **3** | *(请补充实际组件，如 Flink 同步任务)* | | | | | |
| **4** | *(请补充实际组件，如 Hive Metastore)* | | | | | |
| **5** | *(请补充实际组件，如 BI 报表直连服务)* | | | | | |
| **6** | *(请补充实际组件，如 历史小文件合并 Compaction)* | | | | | |
| **7** | *(请补充其他数据抽取/备份/运维脚本)* | | | | | |
| **...**| *(可根据实际情况继续增加行数)* | | | | | |

---

## 二、4 个核心落地必答问题

| # | 核心确认事项 | 详细问题与背景说明 | 填写回复 |
|:---:|---|---|---|
| **1** | **管控范围边界确认** | 本次 Managed Folder 冷热隔离策略，是否确认**仅针对**核心大表 `ods.db/ods_can_origin_data_1s_p_t_i` 生效？之前讨论的其他通用表（如标准 `dt=YYYY-MM-DD` 表）本次是否暂不纳入、保持原样？ | |
| **2** | **服务账号共用情况** | 盘点表中列出的各个组件，目前是否存在共用同一个 GCP Service Account（如 `fr-xjsy-bigdata-dev@...`）的情况？<br>*(注：分析师日常查询账号与后台 ETL 写入账号后续必须支持物理分离)* | |
| **3** | **冷热分区时间基准** | 表内存在多个日期层级，60 天冷热隔离具体按照哪一个分区字段的时间戳进行判定？<br>选项：`server_dt_utc`（服务端时间） 还是 `car_dt_utc`（车端时间）？ | |
| **4** | **历史数据治理作业确认** | 该表是否有 Compaction（小文件合并）、Expire Snapshots 或重写 Manifest 等定期维护作业？<br>*(注：若有，该类作业必须分配“冷+热”读写权，否则合并历史分区时会被拦截)* | |

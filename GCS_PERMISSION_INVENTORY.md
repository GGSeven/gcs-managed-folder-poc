# GCS 访问路径与组件权限盘点表

> **目标存储桶**：`gs://fr-xjsy-bigdata-gcs-dev`  
> **填写目的**：梳理实际访问该存储桶的业务组件与路径范围，为 Managed Folder 冷热数据访问隔离策略提供配置依据。

---

### 填写说明与建议

1. **组件范围说明**：
   - 表中前 7 行系根据前期沟通中提供的 Kyuubi / Spark 配置信息提取预填；
   - 其余组件（如 Trino、Flink、Hive Metastore、各类 ETL 作业等）为**大数据常见架构参考项，并非贵司环境的全量清单**；
   - **请根据贵司实际生产情况自由增删或修改**：实际未使用的组件可直接标记为“不涉及”或删除；若有表中未列出但实际访问 GCS 的其他组件（如特定同步工具、报表系统、离线脚本等），请自行添加行补充完整。
2. **读/写**：按实际需要填写 `读`、`写` 或 `读写`；
3. **数据范围（冷/热）**：
   - 仅针对本次管控目标表（`ods_can_origin_data_1s_p_t_i`）填写：
     - `热`：仅需访问近 60 天内数据；
     - `冷`：仅需访问 60 天前历史数据；
     - `冷+热`：全生命周期数据均需访问；
   - 公共依赖、系统日志或其他业务库表统一填写 `—`。

---

## 一、GCS 访问路径与组件权限盘点表

| 序号 | 业务场景 / 组件名称 | 访问的目标路径（桶内前缀） | 操作类型 | 数据范围 | 当前运行使用的 Service Account | 说明 / 确认状态 |
|:---:|---|---|:---:|:---:|---|:---:|
| **1** | **Kyuubi Server** | `user/spark/kyuubi-spark-sql-engine_2.12-1.9.3.jar` | 读 | — | *(请核对/填写)* | 基于现有配置预填，待核对 |
| **2** | **Spark 引擎（Kyuubi 拉起）** | `user/spark/`（pod 模板） | 读 | — | *(请核对/填写)* | 基于现有配置预填，待核对 |
| **3** | **Spark 引擎公共依赖** | `flink_jar/iceberg-hive-runtime-1.4.2.jar` | 读 | — | *(请核对/填写)* | 基于现有配置预填，待核对 |
| **4** | **Spark 引擎（其他业务表）** | `datasets/`（除管控表外的 warehouse） | 读写 | — | *(请核对/填写)* | 基于现有配置预填，待核对 |
| **5** | **Spark 临时文件上传** | `datasets/`（`spark.kubernetes.file.upload.path`） | 写 | — | *(请核对/填写)* | 基于现有配置预填，待核对 |
| **6** | **Spark 运行时日志** | `spark-job-history/`（`spark.eventLog.dir`） | 写 | — | *(请核对/填写)* | 基于现有配置预填，待核对 |
| **7** | **Spark History Server** | `spark-job-history/`（历史作业回溯） | 读 | — | *(请核对/填写)* | 基于现有配置预填，待核对 |
| **8** | **Spark 引擎（交互查询）** | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/data/` | 读 | *(热 / 冷 / 冷+热)* | *(请核对/填写)* | 待确认该场景下的实际数据范围 |
| **9** | **Spark 引擎（元数据访问）** | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/metadata/` | 读 | 冷+热 | *(请核对/填写)* | 元数据需全量可读 |
| **10** | **Trino** | *(请填写实际访问路径，如 datasets/ods.db/...)* | *(读/写)* | *(热 / 冷 / 冷+热)* | *(请填写)* | 架构参考项，按实际部署情况填写 |
| **11** | **ETL 写入作业（目标表）** | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/` | *(读/写)* | *(热 / 冷 / 冷+热)* | *(请填写)* | 架构参考项，负责写入该表的程序 |
| **12** | **Flink 任务（若有）** | *(请填写实际访问路径)* | *(读/写)* | *(热 / 冷 / 冷+热)* | *(请填写)* | 架构参考项，如无此组件可填“不涉及” |
| **13** | **Hive Metastore（若有）** | *(请填写实际访问路径)* | *(读/写)* | — | *(请填写)* | 架构参考项，如无此组件可填“不涉及” |
| **14** | **其他组件/同步脚本（自定义）** | *(请补充实际存在的其他组件与路径)* | *(读/写)* | *(热 / 冷 / 冷+热)* | *(请填写)* | 自行补充项 |

---

## 二、架构与策略确认事项

| # | 确认事项 | 说明 | 客户填写回复 |
|:---:|---|---|---|
| **1** | **管控范围确认** | 本次 Managed Folder 权限隔离策略，是否确认仅针对核心表 `ods.db/ods_can_origin_data_1s_p_t_i` 生效？存储桶内其他未列出的通用表是否保持现状、不纳入本次隔离范围？ | |
| **2** | **运行身份（Service Account）** | 上表中涉及的各组件，实际分别使用的是哪个 Service Account？是否存在查询类组件与写入类组件共用同一账号的情况？ | |
| **3** | **冷热分区判定字段** | 该表包含多级目录，60 天冷热界限是以 `server_dt_utc`（服务端时间）还是 `car_dt_utc`（车端时间）作为计算基准？ | |
| **4** | **历史治理作业确认** | 该表是否存在定期执行的 Compaction（小文件合并）、Expire Snapshots 等维护作业？（若存在，此类作业通常需要具备历史冷数据的读写权限） | |

# GCS 访问权限盘点与接入确认表（客户现场填写）

> **目标存储桶**：`gs://fr-xjsy-bigdata-gcs-dev`  
> **填写目的**：全面界定贵司生产环境中所有会访问 GCS 的服务组件与作业清单，确保冷热隔离策略生效的同时，不影响生产任务与引擎拉起。

---

### ⚠️ 重要填写提示（必读）
> 1. **切勿局限于本表预设项**：
>    - 表中**【第一部分】**是根据贵司此前提供的配置预填的公共依赖，请直接核对；
>    - 表中**【第二部分】**列出的组件（如 Trino、Flink、DolphinScheduler、HMS 等）**仅为业界常见大数据链路的示例与参考，绝非贵司生产环境的完整列表**。
> 2. **请按贵司真实生产链路【自由增删、修改】**：
>    - 如果某些预设组件贵司根本没有（如未使用 Flink），**请直接删除该行或填“无”**；
>    - 如果贵司有其他自研程序、同步任务（如 DataX、自定义 Python 脚本、特定调度任务名、BI 报表工具等），**请务必自行新增行补齐**；
>    - 核心原则：**只要该程序会向 `gs://fr-xjsy-bigdata-gcs-dev` 发起读取或写入，就必须登记在册**。

---

### 📋 字段填写规范
- **读/写**：填 `读`、`写` 或 `读写`；
- **数据范围（仅针对管控表）**：
  - 若访问的是本次管控大表（`datasets/ods.db/ods_can_origin_data_1s_p_t_i/`）：
    - 填 `热`：仅需查询 60 天内数据（如日常分析）；
    - 填 `冷`：仅需查询 60 天前数据；
    - 填 `冷+热`：历史全量均需访问（如写入、回溯跑批、小文件合并）；
  - 若访问的是公共依赖或非管控表，直接填 `—`；
- **运行凭据**：请填写该组件在运行时使用的 GCP Service Account 账号（或 keyfile 绑定的账号）。

---

## 一、系统与公共运行依赖项（根据贵司现有 Kyuubi/Spark 配置预填）

| 序号 | 组件 / 依赖项名称 | 访问的目标目录（桶内前缀路径） | 操作类型 | 数据范围 | 当前运行凭证 (SA) | 核对状态 |
|:---:|---|---|:---:|:---:|---|:---:|
| **1** | **Kyuubi Server** | `user/spark/kyuubi-spark-sql-engine_2.12-1.9.3.jar` | 读 | — | *(请核对)* | ✔ 预填待核对 |
| **2** | **Spark 引擎（Kyuubi 拉起）** | `user/spark/`（含 pod 模板 ×2） | 读 | — | *(请核对)* | ✔ 预填待核对 |
| **3** | **Spark 引擎公共依赖** | `flink_jar/iceberg-hive-runtime-1.4.2.jar` | 读 | — | *(请核对)* | ✔ 预填待核对 |
| **4** | **Spark 引擎（其他通用表）** | `datasets/`（除本次管控核心表外的 warehouse） | 读写 | — | *(请核对)* | ✔ 预填待核对 |
| **5** | **Spark 临时文件上传** | `datasets/`（`spark.kubernetes.file.upload.path`） | 写 | — | *(请核对)* | ✔ 预填待核对 |
| **6** | **Spark 运行时日志写入** | `spark-job-history/`（`spark.eventLog.dir`） | 写 | — | *(请核对)* | ✔ 预填待核对 |
| **7** | **Spark History Server** | `spark-job-history/`（历史服务回溯读取） | 读 | — | *(请核对)* | ✔ 预填待核对 |

---

## 二、生产业务组件与作业清单（【示例模板】，请根据贵司实际链路增、删、改）

> 💡 **请贵司运维/大数据团队全面排查**：以下行仅作参考，请结合现场调度平台（如 DolphinScheduler）、实时任务、即席查询工具等真实情况，**增补或删除**。

| 序号 | 业务组件 / 调度任务名 | 典型业务场景说明 | 访问的目标目录（桶内前缀） | 读/写 | 数据范围<br>(热/冷/全量) | 当前运行凭据<br>(Service Account) | 实际状态<br>(存在/不存在/已修改) |
|:---:|---|---|---|:---:|:---:|---|:---:|
| **8** | **Spark SQL（交互分析）** | Hue / 分析师提交的即席查询 | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/data/` | 读 | **热** | *(请填写)* | 📝 示例待确认 |
| **9** | **Spark SQL（元数据）** | 读取 Iceberg 结构与快照 | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/metadata/` | 读 | **冷+热** | *(请填写)* | 📝 示例待确认 |
| **10** | **Trino 交互查询** | 业务日常快速 SQL 查询 | *(请填写 Trino 实际访问的前缀)* | 读 | **热** | *(请填写)* | 📝 示例待确认 |
| **11** | **ETL 离线跑批任务**<br>*(如 DolphinScheduler 作业)* | 向核心表写入/同步分区数据的程序（如 `task-icebergdatasync-*`） | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/` | 读写 | **冷+热** | *(请填写写入 SA)* | 📝 示例待确认 |
| **12** | **实时写入作业**<br>*(如 Flink / Kafka Connector)* | 实时摄入车联网 CAN 数据的作业（若有） | *(请填写实时任务写入的前缀)* | 写 | **热** | *(若有请填写)* | 📝 若无请填“无” |
| **13** | **数据湖治理维护作业**<br>*(如 Compaction 小文件合并)* | 定期执行 expire snapshots 或 rewrite manifests 的治理任务 | `datasets/ods.db/ods_can_origin_data_1s_p_t_i/` | 读写 | **冷+热** | *(若有请填写)* | 📝 若无请填“无” |
| **14** | **元数据服务 (HMS)** | Hive Metastore 巡检或元数据同步 | *(请填写 HMS 访问的前缀)* | *(读/写)* | — | *(若有请填写)* | 📝 若无请填“无” |
| **15** | **【贵司自增组件 1】** | *(例如：DataX 同步 / 导出任务)* | | | | | ➕ 请贵司补充 |
| **16** | **【贵司自增组件 2】** | *(例如：自研 Python 巡检脚本 / BI 报表)* | | | | | ➕ 请贵司补充 |
| **17** | **【贵司自增组件 3】** | *(例如：其他大数据调度或消费作业)* | | | | | ➕ 请贵司补充 |

---

## 三、4 个核心落地必答问题

| # | 核心确认事项 | 详细问题与背景说明 | 客户填写回复 |
|:---:|---|---|---|
| **1** | **管控范围边界确认** | 本次 Managed Folder 冷热隔离策略，是否确认**仅针对**核心大表 `ods.db/ods_can_origin_data_1s_p_t_i` 生效？之前讨论的其他通用表（如标准 `dt=YYYY-MM-DD` 表）本次是否暂不纳入、保持原样？ | |
| **2** | **服务账号映射与分离** | 上表中各个组件当前分别使用的是哪个 Service Account？哪些组件目前在共用同一个 SA？<br>*(注：分析师日常查询账号与后台 ETL 写入账号后续必须物理分离)* | |
| **3** | **冷热分区时间基准** | 表内存在多个日期层级，60 天冷热隔离具体按照哪一个分区字段的时间戳进行判定？<br>选项：`server_dt_utc`（服务端时间） 还是 `car_dt_utc`（车端时间）？ | |
| **4** | **历史数据治理作业确认** | 该表是否有 Compaction（小文件合并）、Expire Snapshots 或重写 Manifest 等定期维护作业？<br>*(注：若有，该类作业必须分配“冷+热”读写权，否则合并历史分区时会被拦截)* | |
